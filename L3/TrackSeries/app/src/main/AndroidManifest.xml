<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.example.corina.trackseries">

    <uses-permission android:name="android.permission.INTERNET"/>

    <!-- To auto-complete the email text field in the login form with the user's emails -->
    <uses-permission android:name="android.permission.GET_ACCOUNTS"/>
    <uses-permission android:name="android.permission.READ_PROFILE"/>
    <uses-permission android:name="android.permission.READ_CONTACTS"/>
    <uses-permission android:name="android.permission.VIBRATE"/>

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity
                android:name=".auth.LoginActivity"
                android:theme="@style/LoginPage">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
        <activity
                android:name=".MainActivity"
                android:theme="@style/LoginPage"/>
        <activity
                android:name=".auth.SignupActivity"
                android:label="@string/title_activity_login"
                android:theme="@style/LoginPage"/>
        <activity
                android:name=".auth.ResetPasswordActivity"
                android:label="@string/title_activity_reset_password"
                android:theme="@style/LoginPage"/>
        <activity
                android:name=".AdminActivity"
                android:label="@string/title_activity_admin"
                android:theme="@style/AppTheme">
        </activity>
        <activity
                android:name=".UserActivity"
                android:label="@string/title_activity_user"
                android:theme="@style/AppTheme">
        </activity>
        <activity android:name=".auth.AccountActivity">
        </activity>
        <activity android:name=".show.AddActivity">
        </activity>
        <activity
                android:name=".SettingsActivity"
                android:label="@string/title_activity_settings">
        </activity>

        <receiver android:name=".CheckNotification">
            <intent-filter>
                <action android:name="cwori.action.DISPLAY_NOTIFICATION"/>
            </intent-filter>
        </receiver>

        <activity android:name=".show.ShowActivity">
        </activity>
        <activity android:name=".show.ShowListActivity">
        </activity>
    </application>

</manifest>